
apps/web.zip

# Catalog ingestion run artifacts (SAFE/REVIEW/REJECT deltas)
data/catalog-ingestion/runs/**
!data/catalog-ingestion/runs/.gitkeep

# Incremental adapter state (fingerprints; may grow)
data/catalog-ingestion/state/**
!data/catalog-ingestion/state/.gitkeep

# Source response cache (TTL bodies; may grow)
data/catalog-ingestion/cache/**
!data/catalog-ingestion/cache/.gitkeep


# Verifier scratch output (never committed)
apps/web/TALEPO-GLOBAL-PRODUCT-IDENTITY-V1-TEST-OUTPUT.txt

# GetLayers lisanslı sahne varlıkları repoya girmez (lisans sınırı).
apps/web/public/planet/
