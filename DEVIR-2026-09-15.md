# Devir notu — 2026-09-15 (Tuğrul için)

Bu dalda (`integration/talepo-dev`) 16 commit birden geliyor. Hepsi Dinçer'in
makinesinde yazıldı ve bugüne kadar push edilmemişti. Çoğu lansman öncesi
kapatılan açıklar. Aşağıdakiler **çekmeden önce bilmen gerekenler**.

---

## 1. Merkezî motoru değiştiren tek commit: `0dd20a4`

`apps/web/src/lib/ai/parser/category.ts` — kategori tespitinin kalbi.
**Aynı dosyada çalışıyorsan önce burayı oku.**

Ölçülen kusur: ev bağlamını anlatan sözcükler, aranan şeyin kategorisini
eziyordu. "50 metrekare laminat parke" ve "120 metrekare için parke döşeme"
**EMİN olarak emlağa** düşüyordu — kart bile çıkmıyor, kullanıcı
düzeltemiyordu.

Üç kural eklendi:
- Ölçü birimi (`metrekare`, `m2`) tek başına mülk kanıtı değildir; mülk çıpası
  ya da oda deseni ister.
- İyelik ekinin ÜSTÜNE gelen hâl ekleri de bağlamdır ("evimin" = "evim" kadar
  bağlam). Çıplak "evde"/"daireye" bilerek dışarıda bırakıldı.
- "X için Y" yapısında sol taraf bağlamdır; sağda emlak sinyali varsa hiçbir
  şey düşülmez (fail-closed).

Ölçüm: ev bağlamı korpusunda sorunlu 6/20 → 3/20, kalan üçün hiçbiri sessiz
değil. `verify-request-routing-matrix-v1` 5944/5944 değişmedi.

## 2. Davranışı gerçekten değiştiren diğer commitler

- **`89b656d`** — `updateRequest` artık firma dağıtımı koşuyor. Önceden DRAFT
  bir talep düzenlemeyle yayına geçtiğinde ve kategorisi düzeltildiğinde
  **hiçbir tedarikçiye ulaşmıyordu** (ölçüldü: RequestMatch 0, bildirim 0).
  Kapsam dar: yalnız yayına geçişte ya da `categoryId` değiştiğinde. Başlık
  düzeltmesi ikinci dalga göndermez.
- **`5d341f9`** — `backfillMatchesForAllCompanies` aynı hatayı iki kez
  kaydediyor ve `failureStage`'i tahmin ediyordu. Span sözleşmesi
  (`started` = `completed` + `failed`) kırılıyordu. Dönüş değerine `failed`
  eklendi (additive).
- **`5ddf11b`** — e-posta gönderimi artık gerçek (Resend/Brevo, **yeni
  bağımlılık yok**, `fetch` ile) + şifre sıfırlama akışı. **Şema
  değişmedi**: jeton imzalı ve kullanıcının o anki şifre özetinin parmak izini
  taşıyor, şifre değişince kendiliğinden ölüyor.
- **`405ebdd`** — abonelik iptali (dönem sonu) + kaçan webhook kurtarma.
  Kurtarma ikinci bir yetki değil: sağlayıcının cevabı webhook'un kullandığı
  aynı `applyCanonicalBillingEvent` kapısından geçiyor.
- **`ff2588f`** — teklif fotoğrafı sunucusuzda varsayılan olarak `inline`
  (Vercel'de dosya kayboluyordu); kapak araması ve ödeme çağrısına zaman
  aşımı; `CRON_SECRET` env kataloğuna eklendi.
- **`35aaf1b`** — KVKK aydınlatma metni ve kullanım koşulları yeniden yazıldı.

## 3. Doğrulayıcılarda değişen beklenti — dikkat

`5293339` iki bayat kontrolü düzeltti. `verify-fanout-telemetry-v1` içindeki
"prisma çağrı yüzeyi" kontrolü artık **tutamaç adından bağımsız** ölçüyor
(`prisma.` / `db.` / `tx.` hepsi aynı sayılıyor) ve ayrıca "backfill tekil
`prisma`ya uzanmasın" diye YENİ bir kontrol var. Backfill'de tekil `prisma`
kullanırsan kırmızı alırsın — bu kasıtlı: o kod veritabanı olmadan
ölçülebilir kalmalı.

`verify-log-sink-chain-v1`'in D bölümü artık kanal başına **kurulu** sink
sayıyor ve sayı belgelenen durumdan (her iki kanalda 0) saparsa **kırmızı
veriyor**. Bir sink kurarsan `DOCUMENTED_INSTALLED` değerini güncellemen ve
durumu belgede kapatman gerekir.

## 4. Yeni ortam değişkenleri

Hiçbiri zorunlu değil ama **girilmeden ilgili özellik sessizce çalışmaz**:

| Değişken | Yoksa ne olur |
|---|---|
| `EMAIL_PROVIDER` (`resend`/`brevo`), `EMAIL_API_KEY`, `EMAIL_FROM` | Hiçbir e-posta gitmez; **şifre sıfırlama bağlantısı da gitmez** |
| `APP_BASE_URL` | E-postadaki bağlantılar yanlış adrese gidebilir |
| `CRON_SECRET` | 4 zamanlanmış iş 401 ile sessizce hiç çalışmaz |
| `OFFER_MEDIA_STORAGE` | Artık sunucusuzda otomatik `inline`; açıkça `fs` yazarsan eski davranış |

## 5. Batarya durumu

tsc 0 · eslint 0 · routing-matrix **5944/5944** · fanout-telemetry **70/0**
(65/4 idi) · log-sink 11/0 · offer-media 63/0 · phase4a 24/0 · phase4c 35/0 ·
phase4d 47/0 · state-transition 100/0 · notifications 51/0 · matching-v3 117/0 ·
hybrid-ui 97/0 · public-home PASS · p1-closed-beta 41/0.

**Bilinen kırmızılar değişmedi** — bunlar bu devirle gelmedi, önceden vardı:
`verify-understanding-invariants-v1` 123/6/1 · `verify-category-coverage-v1` 100/8.

## 6. Bu devirde OLMAYAN, hâlâ açık işler

Tedarikçi taksonomi derinliği (migration onayı bekliyor) · logger'a kurulu sink
(ölçüm tabanı hâlâ yok) · hız sınırı bellek içi (çok örnekli kurulumda etkisiz) ·
öne çıkarma para almıyor ve `featuredUntil` hiçbir sorguda okunmuyor · ek teklif
paketlerinin satın alma yüzeyi yok · kurumsal paketin 5 özelliği hiçbir
kullanıcıya açılamıyor · panel'de 27 sayfa hata anında tüm kabuğu kaybediyor ·
belirsiz talepler için triyaj yüzeyi yok.

Ayrıntı (dosya:satır ile) Dinçer'in Veyra klasöründe:
`projects/talepo/LANSMAN-KARNESI-2026-09-15.md` ve
`projects/talepo/LANSMAN-BLOKERLERI-KAPANDI.md`.
