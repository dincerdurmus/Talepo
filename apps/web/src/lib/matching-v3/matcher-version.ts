/** Shadow matching engine identity — not wired to live notifications. */
export const MATCHER_VERSION = "matching-v3/shadow/0.1.0" as const;
export const MATCHER_MODE = "shadow" as const;

export const CALIBRATION_STATUS = "uncalibrated" as const;
